# 🍽️ DishApp - Food Ordering Dashboard

A modern, responsive React application for browsing, searching, and filtering food dishes. Built as part of a frontend development assessment.

---

## ✨ Features Implemented
* **🔐 Authentication:** Secure Login and Signup using Firebase Auth.
* **🔎 Smart Search:** Instant client-side search by dish name.
* **🥗 Filters:** Toggle between **Veg** and **Non-Veg** categories.
* **📶 Sorting:** Sort dishes by **Price (Low/High)** and **Top Rated**.
* **📱 Responsive Design:** Fully responsive grid layout for mobile and desktop.
* **☁️ Cloud Integration:** Connected to Firebase Firestore (with fallback to dummy data if DB is empty).

## 🛠️ Tech Stack
* **Frontend:** React.js + Vite
* **Styling:** CSS3 (Custom Responsive Layouts)
* **Backend / DB:** Firebase Authentication & Cloud Firestore

## ⚙️ How to Run Locally

1.  **Clone the repository:**
    ```bash
    git clone [https://github.com/Chandraveer7389/2025_dishes.git](https://github.com/Chandraveer7389/2025_dishes.git)
    cd dish-app
    ```

2.  **Install Dependencies:**
    ```bash
    npm install
    ```

3.  **Start the Server:**
    ```bash
    npm run dev
    ```

4.  **Login Credentials (for testing):**
    * You can create a new account via the **Sign Up** button.
    * Or use any valid email format (e.g., `test@example.com`).

---

### 📂 Add your own firebase api in the empty section
